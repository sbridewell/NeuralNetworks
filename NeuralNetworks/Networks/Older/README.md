# Older folder

Contains older versions of the neural network implementations, particularly where I've made significant breaking changes. These are not maintained and I'll probably delete them at some point.